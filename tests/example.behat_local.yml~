default:
  extensions:
    Behat\MinkExtension\Extension:
            base_url: {your-localhost-url}

    Drupal\DrupalExtension\Extension:
      drush:
              alias: {your-drush-alias}
      api_driver: drush

imports:
  - ./behat.yml
