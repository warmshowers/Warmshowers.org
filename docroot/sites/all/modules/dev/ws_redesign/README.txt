This module/package is only for the redesign migration, and should be obsolete after that.

It will contain feature modules and the main module will be the place for hook_update_N() things that we can't get
into feature modules.

